package tech.claudioed.authorization.domain;

import io.quarkus.test.junit.SubstrateTest;

@SubstrateTest
public class NativeresourcesIT extends authorizationTest {

    // Execute the same tests but in native mode.
}